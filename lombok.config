# Устанавливаем уровень доступа полей по умолчанию как private
lombok.fieldDefaults.defaultPrivate = true

# Включаем возможность цепочек сеттеров
lombok.accessors.chain = true

# Добавляем аннотацию @lombok.Generated
lombok.addLombokGeneratedAnnotation=true

config.stopBubbling = true